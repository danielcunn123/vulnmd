# vulnmd
Import CherryTree-style JSON, build a linked Markdown vulnerability repository,  fetches vulnerable and fixed Git trees, creates deterministic patches/reports, and optionally asks Gemini CLI to summarize advisories.
